<p id="notice"><%= notice %></p>

<p>
  <strong>List:</strong>
  <%= @task.list_id %>
</p>

<p>
  <strong>Name:</strong>
  <%= @task.name %>
</p>

<%= link_to 'Edit', edit_list_task_path(@list, @task) %> |
<%= link_to 'Back', list_tasks_path(@list) %>
